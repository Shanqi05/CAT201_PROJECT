# BookShelter: Secondhand Bookstore (CAT201 Project)

A bookstore ecommerce website using Java (Backend) and React (Frontend).

## 🚀 How to Run the Project

### 1. Backend (Java/Tomcat)
1. Open the project in **IntelliJ Ultimate**.
2. Make sure you have **Tomcat 10** configured.
3. Click the **Green Play Button** to start the server on `http://localhost:8080`.

### 2. Frontend (React)
The frontend is located in the `secondbook-frontend` folder.
You must switch folders to run it!

```bash
cd secondbook-frontend
npm install
npm run dev